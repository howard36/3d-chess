# Verification: foundations

How to run this file: bring up the local server and client as in [the protocol](README.md#how-to-run-a-pass). Unless a row says otherwise, the setup is a **new game with both seats taken**: create a game in one browser context, join it from a second, and note which page is White (the seat label says "You are playing as white."). Leave the view at the default (reload to reset it) in a 1280 × 720 window. "Press" means a left click (or a tap) on the board, released where it went down. Device values: `mouse`, `keyboard`, `touch`, `second player` (the other context), `second tab` (same context as the player), `drop` (see [devices and conditions](README.md#devices-and-conditions)), `narrow`. Several expected results are read back from the console handle described in [the protocol](README.md#driving-the-product-from-a-console-or-script); those rows say so.

## foundations/game-rules.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RULES-01 | P1 | mouse | Each side starts with 20 pieces placed as the table shows ([the starting position](../foundations/game-rules.md#the-starting-position)). | New game, White's page. | 1. In the console, list every piece's type, color, and cell (or orbit the view and read the board). | 40 pieces. White: `Aa1` R, `Ab1` N, `Ac1` K, `Ad1` N, `Ae1` R; `Ba1` B, `Bb1` U, `Bc1` Q, `Bd1` B, `Be1` U; Pawns on rank 2 of levels A and B. Black: `Ea5` R, `Eb5` N, `Ec5` K, `Ed5` N, `Ee5` R; `Da5` U, `Db5` B, `Dc5` Q, `Dd5` U, `De5` B; Pawns on rank 4 of levels E and D. Level C and rank 3 empty. | pass |
| RULES-02 | P2 | second player | White moves first ([the starting position](../foundations/game-rules.md#the-starting-position)). | New game. | 1. Read both turn indicators.<br>2. On Black's page, press a Black pawn. | Both read "White to move". Nothing is selected on Black's page. | pass |
| RULES-03 | P2 | mouse | A pawn steps forward or up, one cell, with no two-cell first move ([pawns](../foundations/game-rules.md#pawns)). | New game, White's page. | 1. Press `Bb2`.<br>2. Press `Ab2`. | `Bb2`: exactly `Bb3` and `Cb2` marked. `Ab2`: exactly `Ab3` (its *up* cell `Bb2` is occupied). `Bb4`/`Ab4` never marked. | pass |
| RULES-04 | P2 | mouse | The Knight jumps two along one axis and one along another ([how the pieces move](../foundations/game-rules.md#how-the-pieces-move)). | New game, White's page. | 1. Press the Knight on `Ab1`. | Exactly six destinations: `Aa3`, `Ac3`, `Bb3`, `Ca1`, `Cb2`, `Cc1`. | pass |
| RULES-05 | P2 | mouse | Sliders stop at the first piece and may capture it ([how the pieces move](../foundations/game-rules.md#how-the-pieces-move)). | New game, White's page. | 1. Press the Queen on `Bc1`. | Exactly 14 destinations: `Cb1`, `Cb2`, `Cc1`, `Cc2`, `Cd1`, `Cd2`, `Da1`, `Da3`, `Dc1`, `Dc3`, `De1`, `De3`, `Ec1`, `Ec4`; `Ec4` (a Black pawn) with a red capture ring. | pass |
| RULES-06 | P2 | mouse | A piece with no legal move can be selected and shows no destinations ([how the pieces move](../foundations/game-rules.md#how-the-pieces-move)). | New game, White's page. | 1. Press the King on `Ac1`. | Gold ring under the King; no destination markers. | pass |
| RULES-07 | P1 | second player | Pawn captures go forward-up, forward-sideways, or up-sideways, never straight ([pawns](../foundations/game-rules.md#pawns)). | Play `Ba2-Ba3`, `Ee4-Ee3`, `Ba3-Ca3`, `Ee3-Ee2`, `Ca3-Da4`, `Ee2-Ee1`. | 1. On White's page press `Da4`. | Exactly `Db5`, `Ea5`, `Eb4`, each a capture ring. No quiet dot (`Da5` and `Ea4` are occupied). | pass |
| RULES-08 | P1 | second player | Reaching rank 5 on a level other than E does not promote ([promotion](../foundations/game-rules.md#promotion)). | As RULES-07. | 1. Press `Da4`, then `Db5`. | No promotion dialog. The move lands as a pawn capture; the move list shows `Da4–Db5` with no `=`; the turn indicator reads "Black to move — in check" (the pawn on `Db5` attacks the King on `Ec5`). | pass |
| RULES-09 | P1 | second player | In check, the King glows red, the turn indicator says so, and only moves that end the check are offered ([check, checkmate, and stalemate](../foundations/game-rules.md#check-checkmate-and-stalemate)). | New game. | 1. White plays `Bc1-Ec4`.<br>2. On Black's page press `Ec5`, then `Dc5`, then `Dd5` (orbit first if it is hidden), then `Ed4`. | Black's King glows red on both pages. Both turn indicators read "Black to move — in check". `Ec5`, `Dc5`, `Dd5` each show exactly one destination, `Ec4`. `Ed4` shows none. | pass (Dd5 hidden from the default view, pressed after orbiting) |
| RULES-10 | P1 | second player | Checkmate ends the game ([check, checkmate, and stalemate](../foundations/game-rules.md#check-checkmate-and-stalemate)). | New game. | 1. Play `Ad1-Cc1`, `Dc5-Bc3`, `Bc1-Ad1`, `Bc3-Ab2`. | Both pages show "Black wins by checkmate!". | pass |
| RULES-11 | P2 | mouse | The board has no coordinate labels ([the board](../foundations/game-rules.md#the-board)). | Any board screen. | 1. Orbit the view all the way round. | No letters, digits, or level names anywhere in the scene. | pass (no sprite, text or textured object in the scene (object types: DirectionalLight, Group, HemisphereLight, LineSegments, Mesh, Scene); orbit screenshots rules-11-orbit-0..3.png) |

Not checkable by hand:

- White's 61 legal first moves, and Black's by symmetry: covered by the engine's unit tests.
- Stalemate: no short line from the starting position is known; covered by the engine's unit tests only.
- The absence of castling, en passant, draw rules, resignation, and a clock is an absence of controls; there is nothing to press.

## foundations/input-model.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| INPUT-01 | P1 | mouse | The board acts on release, not while the button is held ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White's page, White to move, nothing selected. | 1. Press and hold the left button on `Bb2`.<br>2. Look before releasing.<br>3. Release without moving. | 2: nothing is selected yet. 3: the gold ring and two destinations appear on release. | pass |
| INPUT-02 | P1 | mouse | A drag that starts on a legal destination only turns the view ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, `Ab2` selected. | 1. Press on `Ab3`'s dot.<br>2. Drag 200 px left and release. | The view orbits. No move is played: still "White to move", no move list, `Ab2` still selected with its destination. | pass (view orbited; no move played; Ab2 still selected with Ab3) |
| INPUT-03 | P1 | mouse | A drag that starts on an empty part of the board keeps the selection ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, Queen `Bc1` selected. | 1. Press on an empty cell in the middle of the cube (for example over `Cc3`).<br>2. Drag 150 px and release. | The view orbits; the Queen is still selected with its destinations. | pass (view orbited; Queen still selected with its 14 destinations) |
| INPUT-04 | P1 | mouse | A press on the background outside the cube leaves the selection alone ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, `Bb2` selected. | 1. Click the background near the left edge of the window, well clear of the cube. | `Bb2` is still selected with its two destinations. | pass |
| INPUT-05 | P1 | mouse | The right and middle buttons never act on the board; the context menu never opens ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, nothing selected. | 1. Right-click `Bb2` without moving.<br>2. Middle-click `Bb2` without moving.<br>3. Left-click `Bb2`. | 1 and 2: nothing is selected, and no browser context menu appears. 3: `Bb2` is selected. | pass (right and middle click selected nothing; contextmenu events [true] (true = default prevented, so no menu); left click selected Bb2) |
| INPUT-06 | P1 | mouse | The wheel never touches the selection ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | `Bb2` selected. | 1. Turn the wheel both ways over the cube. | The view zooms; `Bb2` stays selected. | pass (distance 14.48 -> 13.76 -> 14.48; Bb2 still selected) |
| INPUT-07 | P1 | mouse | Pressing a piece that cannot be selected clears the selection ([what takes a press](../foundations/input-model.md#what-takes-a-press)). | White to move, `Bb2` selected. | 1. Press the Black pawn `Ed4`. | Nothing is selected. | pass |
| INPUT-08 | P1 | mouse | Pressing another own piece moves the selection ([what takes a press](../foundations/input-model.md#what-takes-a-press)). | `Bb2` selected. | 1. Press `Bc2`. | `Bc2` is selected with its destinations; `Bb2` is not. | pass |
| INPUT-09 | P1 | mouse | A piece in front of a destination blocks it ([what takes a press](../foundations/input-model.md#what-takes-a-press)). | White to move, Queen `Bc1` selected. | 1. Find a highlighted cell partly hidden behind one of White's own pieces.<br>2. Press where that piece covers the highlight. | The piece in front becomes selected; no move is sent. | pass (destination Cb1 blocked by white Pawn; press selected it instead, no move sent) |
| INPUT-10 | P2 | mouse | Empty cells in front of a destination do not block it ([what takes a press](../foundations/input-model.md#what-takes-a-press)). | Queen `Bc1` selected. | 1. Press `Cc2`, seen through empty cells. | The move `Bc1–Cc2` is played. | pass |
| INPUT-11 | P2 | mouse | The turn indicator and the seat label let presses through; the move box does not ([what takes a press](../foundations/input-model.md#what-takes-a-press)). | White to move. Orbit or pan until a White piece is under the turn indicator, another under the seat label, and a third under the move box (bottom left). | 1. Press through the turn indicator on the first piece.<br>2. Press through the seat label on the second.<br>3. Press on the move box's label, over the third. | 1: the first piece is selected. 2: the second piece is selected. 3: the selection does not change. | pass (a press on the turn indicator reaches <CANVAS>, on the seat label <CANVAS>, on the move box's label <LABEL>) |
| INPUT-12 | P1 | drop | The board takes no input while not connected; the view still turns ([when the board takes input](../foundations/input-model.md#when-the-board-takes-input)). | White to move, `Bb2` selected. | 1. Drop White's connection and hold it down.<br>2. Press `Bc2`.<br>3. Drag the view.<br>4. Restore the connection; once "Reconnecting…" is gone, press `Bc2`. | 1: "Reconnecting…" shows and the selection is cleared. 2: nothing is selected. 3: the view turns. 4: `Bc2` is selected. | pass |
| INPUT-13 | P2 | keyboard | Only HTML controls are reachable; no key acts on the board or the view ([HTML controls and the keyboard](../foundations/input-model.md#html-controls-and-the-keyboard)). | Board screen, an error showing (so the banner's ✕ exists), the move box empty. | 1. Press Tab repeatedly.<br>2. Click the empty background, then press the arrow keys, Enter, Space, and Escape. | Tab reaches the move box's text field and "✕" ("Dismiss error"), and nothing else on the page (the "Move" button is disabled while the field is empty). No key selects, moves, or turns the view. | pass (banner "Error: Not your turn✕"; Tab cycle: INPUT#typed-move: -> BUTTON:Dismiss error -> BODY -> INPUT#typed-move: -> BUTTON:Dismiss error -> BODY -> INPUT#typed-move: -> BUTTON:Dismiss error; keys changed nothing) |
| INPUT-14 | P2 | touch | A tap acts when the finger lifts ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, touch device. | 1. Touch and hold `Bb2`.<br>2. Lift the finger without moving. | 1: nothing is selected while the finger is down. 2: `Bb2` is selected. | pass (nothing selected while the emulated finger was down; selected when it lifted) |
| INPUT-15 | P3 | mouse | A drag of 6 pixels or less still counts as a press ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, nothing selected. | 1. Press on `Bb2`, move the pointer 4 px, and release.<br>2. Clear, then press on `Bb2`, move 12 px, and release. | 1: `Bb2` is selected. 2: nothing is selected; the view has turned slightly. | pass (4 px drag selected Bb2; 12 px drag selected nothing and moved the camera 1.37 units) |
| INPUT-16 | P2 | mouse | A right drag across a destination pans and plays nothing ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)). | White to move, `Ab2` selected. | 1. Right-press on `Ab3`'s dot, drag 150 px, and release. | The view pans. No move is played; `Ab2` is still selected. | pass (target panned 2.18 units; no move sent; Ab2 still selected) |
| INPUT-17 | P1 | keyboard | A move typed in the move box is played like a pressed one ([the move box](../foundations/input-model.md#the-move-box)). | White's page, White to move. | 1. Tab to the move box's field.<br>2. Type `ab2 ab3` and press Enter. | The field empties; the pawn glides to `Ab3` on both pages; "Black to move"; the move list shows `1. Ab2–Ab3`. | pass (field emptied; glide seen on both pages; "1. Ab2–Ab3" on both) |
| INPUT-18 | P2 | keyboard | A move that cannot be played is explained and not sent ([the move box](../foundations/input-model.md#the-move-box)). | White's page, White to move. | 1. Type `Ab2-Ab5` and press Enter.<br>2. Replace it with `Ed4-Ed3` and press Enter.<br>3. Replace it with `hello` and press Enter. | 1: "The piece on Ab2 cannot move to Ab5." 2: "You have no piece on Ed4." 3: "Type a move as two cells, like Ab2-Ab3." Each shows under the field; the text stays; still "White to move" on both pages. | pass (Ab2-Ab5: "The piece on Ab2 cannot move to Ab5."; Ed4-Ed3: "You have no piece on Ed4."; hello: "Type a move as two cells, like Ab2-Ab3.") |
| INPUT-19 | P2 | second player | The move box plays only on the player's own turn ([the move box](../foundations/input-model.md#the-move-box)). | White to move. | 1. On Black's page type `Ed4-Ed3`.<br>2. Press Enter. | The "Move" button stays disabled; Enter does nothing; the text stays and nothing is sent. | pass (Move button disabled; Enter did nothing; text kept; no move frame sent) |
| INPUT-20 | P1 | keyboard | The promotion dialog takes focus on "Queen", Escape cancels at once, and the page behind is out of reach ([HTML controls and the keyboard](../foundations/input-model.md#html-controls-and-the-keyboard)). | As RULES-07, White's page. | 1. Press `Da4`, then `Ea5`.<br>2. Read which element has focus.<br>3. Press Tab several times.<br>4. Press Escape. | 1: "Promote to" opens. 2: "Queen". 3: focus never reaches the move box or anything else behind the dialog. 4: the dialog closes; nothing is sent; still "White to move". | pass (focus on open: Queen; Tab cycle: dialog:BUTTON:Rook, dialog:BUTTON:Bishop, dialog:BUTTON:Knight, dialog:BUTTON:Unicorn, dialog:BUTTON:Cancel, BODY, dialog:BUTTON:Queen; Escape closed it, nothing sent) |
| INPUT-21 | P2 | drop | After a reconnect the board takes no input until the rejoin's snapshot arrives ([when the board takes input](../foundations/input-model.md#when-the-board-takes-input)). | White to move. A way to hold the server's messages to White's page after its connection reopens (the harness can delay incoming frames). | 1. Drop White's connection.<br>2. Let it reopen, holding the server's answer back.<br>3. Press `Bb2`; type `Ab2-Ab3` in the move box.<br>4. Release the answer; press `Bb2`. | 3: nothing is selected, and the "Move" button is disabled. 4: `Bb2` is selected. | pass (while the rejoin answer was held (343 ms into an 8 s hold): nothing selected, Move disabled; after: Bb2 selected) |

Not checkable by hand:

- A second finger landing during a one-finger gesture never acts on the board ([a press acts on release](../foundations/input-model.md#a-press-acts-on-release)): needs a real touch device; try it on one if available and file what happens.
- Whether a trackpad pinch arrives as a wheel or as touches, per browser. Neither acts on the board.
- Two presses on a destination before the page redraws both send the move (bug-triage B-14): needs clicks faster than a person can make.

## foundations/connection-and-seat.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CONN-01 | P1 | mouse | The stored seat is written when the game is created ([the stored seat](../foundations/connection-and-seat.md#the-stored-seat)). | Fresh browser context. | 1. Create a game.<br>2. Read local storage for `localhost:5173`. | A key `3dchess:role:{id}` with value `white` or `black`, present as soon as the share-link screen shows. | pass (3dchess:role:WLFMT9 = black on the share-link screen) |
| CONN-02 | P1 | second player | The joiner's stored seat is written on joining ([the stored seat](../foundations/connection-and-seat.md#the-stored-seat)). | A created game. | 1. Join from a second context.<br>2. Read its local storage. | `3dchess:role:{id}` holds the color opposite the creator's. | pass |
| CONN-03 | P1 | drop | The retry schedule is 0.5 s, 1 s, 2 s, 4 s, then every 8 s ([connection states](../foundations/connection-and-seat.md#connection-states)). | Board screen. | 1. Drop the connection and hold it down for 20 s, recording each attempt's time. | Attempts about 0.5, 1, 2, 4, 8, 8 s apart. "Reconnecting…" the whole time. | pass (attempts after drop at +486 ms, then gaps [1002,2002,4002,8001]; "Reconnecting…" throughout) |
| CONN-04 | P1 | drop | The start screen shows "Reconnecting to server…" while reconnecting ([connection states](../foundations/connection-and-seat.md#connection-states)). | Start screen. | 1. Stop the local server.<br>2. Start it again. | "Reconnecting to server…" under the button until it restarts, then nothing. | pass |
| CONN-05 | P1 | second tab | Last connection wins; the old tab is not retried ([last connection wins](../foundations/connection-and-seat.md#last-connection-wins)). | Board screen in tab 1. | 1. Open the same game link in tab 2 of the same context.<br>2. Wait 5 s. | Tab 2 shows the board as the same color. Tab 1 shows "This game is open in another tab" and stays so. | pass |
| CONN-06 | P1 | second player | Presence follows the opponent's connection ([presence](../foundations/connection-and-seat.md#presence)). | Board screen on both pages. | 1. Reload Black's page. | White's page shows "Opponent: offline", then "Opponent: online". | pass (sequence seen: Opponent: online -> Opponent: offline) |
| CONN-07 | P1 | second player | Returning to the start screen resets the connection; Forward rejoins ([returning to the start screen](../foundations/connection-and-seat.md#returning-to-the-start-screen)). | Board screen on both pages; Black arrived at the game from the start screen (a page that opened the link directly leaves the app on Back). | 1. On Black's page press browser Back.<br>2. Then Forward. | 1: Black's page shows the start screen; White's shows "Opponent: offline". 2: Black's page shows the board again; White's shows "Opponent: online". | pass |
| CONN-08 | P1 | mouse | A refused rejoin before any snapshot deletes the stored seat and shows the join screen ([the stored seat](../foundations/connection-and-seat.md#the-stored-seat)). | A game created by this context; the local server restarted since (which deletes the game). | 1. Open the game link. | "Error: Cannot rejoin" in the banner, the "Join Game" button, and the `3dchess:role:{id}` key gone. | pass (the reloaded (never snapshotted) page: join screen, "Error: Cannot rejoin", stored seat deleted) |
| CONN-09 | P1 | mouse | A returning player sees the share-link wording while the rejoin is in flight (suspected copy bug) ([rejoining](../foundations/connection-and-seat.md#rejoining)). | A started game; the joiner's context. Stop the local server. | 1. Reload the joiner's page. | Record what shows. Per the document: "Game created! Share this link with a friend:" with "Reconnecting…". | pass (the reloaded page of a started game showed "Game created! Share this link with a friend:" with "Reconnecting…" (bug-triage B-12, still open)) |
| CONN-10 | P2 | drop | Presence is stale while this player reconnects ([presence](../foundations/connection-and-seat.md#presence)). | Board screen, "Opponent: online". | 1. Drop White's connection and hold it.<br>2. Close Black's context. | White still shows "Opponent: online" until White reconnects, then "Opponent: offline". | pass |
| CONN-11 | P2 | second player | Moves made while the opponent is offline are recorded and shown on return ([presence](../foundations/connection-and-seat.md#presence)). | Board screen, White to move. | 1. Close Black's page.<br>2. White plays `Ab2-Ab3`.<br>3. Reopen the link in Black's context. | White's move lands for White. Black's page shows `Ab3` occupied, the move in the list, "Black to move", with no glide. | pass |
| CONN-12 | P2 | second player | Without the stored seat, a new tab cannot recover the seat ([the stored seat](../foundations/connection-and-seat.md#the-stored-seat)). | Started game. | 1. Clear Black's site data (local and session storage).<br>2. Close Black's tab and open the game link in a new tab of Black's context.<br>3. Click "Join Game". | The join screen, then "Error: Game full". | pass (join screen, then "Error: Game full") |
| CONN-13 | P3 | second tab | Each tab has its own client id, kept across a reload ([the client id](../foundations/connection-and-seat.md#the-client-id)). | Board screen in tab 1. | 1. Read session storage key `3dchess:clientId` in tab 1.<br>2. Reload tab 1 and read it again.<br>3. Open the game in tab 2 of the same context and read it there. | 1 and 2: the same value. 3: a different value. | pass (tab 1 8ee43d71… before and after reload; tab 2 ef3c1b8d…) |
| CONN-14 | P1 | second tab | "Play here" has focus and takes the seat back ([last connection wins](../foundations/connection-and-seat.md#last-connection-wins)). | After CONN-05: tab 1 shows the replaced dialog. | 1. In tab 1, read which element has focus.<br>2. Press Enter. | 1: "Play here". 2: tab 1 shows the board as the same color and takes input once "Reconnecting…" (if shown) is gone; tab 2 shows "This game is open in another tab". | pass (focus on "Play here"; Enter took the seat back; Bb2 selectable after 605 ms; tab 2 shows the dialog) |
| CONN-15 | P1 | drop, second tab | An automatic reconnect does not take the seat from another tab ([last connection wins](../foundations/connection-and-seat.md#last-connection-wins)). | Board screen in tab 1. | 1. Drop tab 1's connection and hold it down.<br>2. Open the game link in tab 2 of the same context and wait for its board.<br>3. Restore tab 1's connection. | 3: tab 1 shows "This game is open in another tab" with "Play here", and no "Error:" banner. Tab 2 keeps the board, with no dialog, and can move. The opponent's page still shows "Opponent: online". | pass (tab 1: dialog with "Play here", no error banner; tab 2 played Ab2-Ab3; opponent sees "Opponent: online") |
| CONN-16 | P1 | drop, second player | A join whose answer is lost is re-sent and gets the seat ([requests while disconnected](../foundations/connection-and-seat.md#requests-while-disconnected)). | A created game; the joiner's context on the join screen. A way to drop the joiner's connection after its join is sent but before the answer arrives (the harness can hold incoming frames, then close the socket). | 1. Click "Join Game".<br>2. Drop the connection before the answer arrives; restore it. | "Joined game, waiting for start..." until the connection returns, then both pages show the board. The joiner holds the color opposite the creator's, with `3dchess:role:{id}` written. No "Game full". | pass (joined screen while down; then both boards; joiner white (creator black), key written; no "Game full") |
| CONN-17 | P2 | drop | A create whose answer is lost is re-sent ([requests while disconnected](../foundations/connection-and-seat.md#requests-while-disconnected)). | Start screen, connected. The same way to lose an answer as CONN-16. | 1. Click "Start New Game".<br>2. Drop the connection before the answer arrives; restore it. | "Creating Game..." (disabled) until the connection returns, then the share-link screen of a new game. | pass ("Creating Game..." disabled while down; then share-link screen of LFM7B2) |
| CONN-18 | P2 | second player | The tab that claimed a seat gets it back by joining again, with the game as it stands ([the client id](../foundations/connection-and-seat.md#the-client-id)). | Started game with `Ab2-Ab3` played; the joiner's tab. | 1. In the joiner's tab, delete only the local storage key `3dchess:role:{id}`.<br>2. Reload the same tab.<br>3. Click "Join Game". | 2: the join screen. 3: the board, as the same color as before, with `Ab3` occupied, "1. Ab2–Ab3" in the move list, and "Black to move"; no "Game full". | pass (joiner (black) got its seat back in the same tab) |

Not checkable by hand:

- The one-hour connection limit and the ~30-day expiry: too long for a pass. Whether a rejoin counts as activity for the expiry is an open question.
- That a move is never shown before its echo: on a local server the echo arrives within a frame. Covered by the unit tests of the game page.
- A duplicated tab sharing its client id, and a creator joining back into an unstarted game from the same tab: read from code, listed as open questions.

## foundations/screens-and-navigation.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| NAV-01 | P1 | second player | Each game page phase shows its screen ([the game page and its phases](../foundations/screens-and-navigation.md#the-game-page-and-its-phases)). | Fresh contexts. | 1. Create a game.<br>2. Open its link in the second context.<br>3. Click "Join Game". | 1: share-link screen. 2: join screen with only "Join Game". 3: "Joined game, waiting for start..." at most briefly, then both pages show the board screen. | pass (join screen buttons ["Join Game"] (text "3D Chess / Join Game"); "Joined game…" seen 0 of 5 polls before the board) |
| NAV-02 | P1 | mouse | Unknown addresses show an empty dark page (suspected bug) ([addresses the app does not know](../foundations/screens-and-navigation.md#addresses-the-app-does-not-know)). | Any state. | 1. Open `http://localhost:5173/games`.<br>2. Open `http://localhost:5173/game/`. | Record what shows. Per the document: an empty dark page, no text, no link. | pass (/games text "" links 0; /game/ text "" links 0; body background oklch(0.21 0.034 264.665)) |
| NAV-03 | P2 | mouse | An id in lower case is an unknown game ([addresses the app does not know](../foundations/screens-and-navigation.md#addresses-the-app-does-not-know)). | A game with id `ABC123` exists (use a real id). | 1. Open `/game/abc123` (the real id in lower case) in a fresh context.<br>2. Click "Join Game". | "Error: Cannot join" and the join screen again. | pass |
| NAV-04 | P1 | second player | "Start new game" adds a history entry; Back returns to the finished game ([moving between pages](../foundations/screens-and-navigation.md#moving-between-pages)). | A finished game (RULES-10). | 1. On White's page click "Start new game".<br>2. Press browser Back. | 1: the start screen. 2: the game page, rejoined, with "Black wins by checkmate!" again. | pass |
| NAV-05 | P2 | mouse | The page title never changes ([moving between pages](../foundations/screens-and-navigation.md#moving-between-pages)). | Any state. | 1. Read the tab title on the start screen, the share-link screen, the board screen, and after the opponent moves. | Always "3D Chess — Online Multiplayer". | pass (start: 3D Chess — Online Multiplayer; share-link: 3D Chess — Online Multiplayer; board: 3D Chess — Online Multiplayer; after opponent move (Black's page): 3D Chess — Online Multiplayer) |
| NAV-06 | P2 | drop | The board screen stays through a reconnect ([the game page and its phases](../foundations/screens-and-navigation.md#the-game-page-and-its-phases)). | Board screen. | 1. Drop and restore the connection. | The board stays up throughout, with "Reconnecting…" meanwhile. | pass |
| NAV-07 | P2 | mouse | The reconnecting box shows on every game-page screen ([the game page and its phases](../foundations/screens-and-navigation.md#the-game-page-and-its-phases)). | Share-link screen. | 1. Stop the local server. | "Reconnecting…" at the top right of the share-link screen. | pass ("Reconnecting…" at x=1103, y=10 on the share-link screen) |
| NAV-08 | P2 | second player | A history jump from one game's page straight to another's shows the other game ([moving between pages](../foundations/screens-and-navigation.md#moving-between-pages)). | A finished game A (RULES-10) on White's page. | 1. Click "Start new game", then "Start New Game" (game B, share-link screen).<br>2. Go back two history entries at once (the Back button's long-press menu, or `history.go(-2)` in the console). | The address is game A's, and the page shows game A: the finished position with "Black wins by checkmate!". No "Error:" banner. Black's page (still on game A) shows "Opponent: online". | pass (from II5NLD back to VAK0EJ: finished game shown, no error, Black sees "Opponent: online") |
| NAV-09 | P2 | keyboard | The end-game dialog takes focus on "Start new game" and blocks the page behind it ([the game page and its phases](../foundations/screens-and-navigation.md#the-game-page-and-its-phases)). | A finished game (RULES-10), White's page. | 1. Read which element has focus.<br>2. Press Tab several times.<br>3. Press Enter on "Start new game". | 1: "Start new game". 2: focus never reaches the move box or the move list. 3: the start screen. | pass (focus on open: "Start new game"; Tab cycle: BODY, dialog:BUTTON:Start new game; Enter -> start screen) |
| NAV-10 | P3 | drop | Back and Forward while the reset connection is still opening send a single rejoin ([edge cases](../foundations/screens-and-navigation.md#edge-cases)). | Share-link screen of a created game. | 1. Hold the connection down (stop the local server's traffic, or use the harness).<br>2. Press Back, then Forward.<br>3. Restore the connection. | The share-link screen, with "Reconnecting…" until the connection returns; afterwards no "Error: Already in a game". | pass (share-link screen with "Reconnecting…" while down; afterwards alerts []) |

Not checkable by hand:

- The crash screen needs an error the product does not normally hit; it is covered by a unit test. Whether a browser without 3D support reaches it is an open question.

## foundations/the-view.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| VIEW-01 | P1 | second player | Each player sees their own side: back rank at the bottom of the nearest slice, files mirrored for Black ([orientation](../foundations/the-view.md#orientation)). | New game. | 1. Compare the two pages side by side. | Both show the same picture: own Rook–Knight–King–Knight–Rook along the bottom of the nearest slice, own pawns above, the opponent's army at the top of the far slices. White's `Aa1` Rook is bottom-left; Black's `Ee5` Rook is bottom-left. | pass (White's Aa1 and Black's Ee5 project to the same pixel (437,503); a->e runs left to right for White, a on the right for Black) |
| VIEW-02 | P1 | mouse | Moving up a level moves away from the viewer ([orientation](../foundations/the-view.md#orientation)). | White to move. | 1. Play `Bb2-Cb2`. | The pawn glides deeper into the screen, not up it. | pass (screen shift of the step up a level: dx=48 dy=-27 px (depth; see screenshots)) |
| VIEW-03 | P2 | second player | Both players' cameras start in the same direction and at the same distance for the same window size ([the default view](../foundations/the-view.md#the-default-view)). | New game, both windows 1280 × 720. | 1. Read the camera position on each page. | The same position on both, a multiple of `[6.5, 5, 8.5]` (the direction), about 14.5 units from the center. | pass (both at [7.970, 6.131, 10.422], 14.48 units from the target) |
| VIEW-04 | P2 | mouse | Left drag orbits, right drag pans; Shift, Ctrl, or Cmd swaps them ([turning the view](../foundations/the-view.md#turning-the-view)). | Board screen, press on the background. | 1. Left-drag.<br>2. Right-drag.<br>3. Shift+left-drag.<br>4. Shift+right-drag. | 1 and 4 move the camera around a fixed point; 2 and 3 move the point too. | pass (target moved: orbit 0.000, pan 1.74, shift+left 1.75, shift+right 0.014) |
| VIEW-05 | P3 | mouse | In a wide window, zoom stays between 6 and 25 units ([turning the view](../foundations/the-view.md#turning-the-view)). | Board screen, 1280 × 720. | 1. Wheel in fully.<br>2. Wheel out fully. | Camera distance from its target stops at 6 and at 25. | pass (stops at 6.00 and 25.00) |
| VIEW-06 | P3 | mouse | The view coasts briefly after a drag ([turning the view](../foundations/the-view.md#turning-the-view)). | Board screen. | 1. Left-drag quickly and release. | The view keeps turning for a moment after release. | pass (camera still moving 300 ms after release) |
| VIEW-07 | P2 | second player | The view is not reset by moves, but is by a reload ([the default view](../foundations/the-view.md#the-default-view)). | Board screen, orbited away from default. | 1. Let the opponent move.<br>2. Reload. | 1: the angle is kept. 2: back to the default view. | pass (angle kept through the opponent's move (camera moved 0.008); after reload back at the default direction, 14.48 units) |
| VIEW-08 | P3 | mouse | Marker colors ([markers and colors](../foundations/the-view.md#markers-and-colors)). | A capture available (RULES-05). | 1. Select the Queen `Bc1`. | Gold ring under the Queen, amber tint and dots on empty destinations, red ring at `Ec4`. After a move: teal tint on its two cells. | pass (marker colors: dot #ffb020, capture #ff5d5d, select #ffc247 (screenshot markers.png)) |
| VIEW-09 | P2 | second player | A destination's amber fill replaces the teal last-move fill on a shared cell ([markers and colors](../foundations/the-view.md#markers-and-colors)). | Play `Ab2-Ab3`, `Ed4-Ed3`. `Ed4` and `Ed3` are teal. | 1. On White's page select the Bishop `Bd1`. | `Ed4`, the last move's origin and one of the Bishop's destinations, shows the amber tint, not teal; `Ed3` stays teal. | pass (Bishop destinations Cc1,Cd2,Ce1,Db1,Dd3,Ea1,Ed4; teal cells now 1 (Ed3 only); Ed4 amber) |
| VIEW-10 | P1 | second player | Every new move glides on both boards, the mover's too; a captured piece fades ([motion](../foundations/the-view.md#motion)). | New game. | 1. White plays `Bc1-Ec4` (a capture). | On both pages the Queen glides over about 0.3 s and the Black pawn on `Ec4` shrinks away. | pass (glide and fading piece on both pages; fade finished 1307 ms after the press at 9 frames/s (software rendering)) |
| VIEW-11 | P1 | second player | Moves already in the record do not glide when the board appears ([motion](../foundations/the-view.md#motion)). | After VIEW-10. | 1. Reload White's page. | The Queen is on `Ec4` at once, teal on `Bc1` and `Ec4`, no glide. | pass |
| VIEW-12 | P2 | mouse | Pan has no limit; the board can leave the screen ([turning the view](../foundations/the-view.md#turning-the-view)). | Board screen. | 1. Right-drag repeatedly in one direction. | The cube slides off screen; nothing brings it back but panning back or reloading. | pass (board centre now at (2321,252) in a 1280x720 window) |
| VIEW-13 | P1 | mouse | The default view frames the whole cube ([the default view](../foundations/the-view.md#the-default-view)). | New game, 1280 × 720 window. | 1. Project the eight corners of the lattice (outermost cells plus half a cell) to the screen, or look at the nearest bottom edge. | All eight corners are inside the window; the nearest pieces' feet are visible. | pass (corners span x 349..911, y 121..688 in 1280x720; lowest foot at y 649) |
| VIEW-14 | P2 | narrow | In an upright phone-sized window the whole cube is in frame ([the default view](../foundations/the-view.md#the-default-view)). | New game, a 375 × 667 window. | 1. Read the camera's distance from its target and look at the sides of the cube.<br>2. Wheel out fully. | 1: about 21.7 units; no part of the cube is cut off at the sides. 2: the camera stops at about 32.6 units. | pass (fitted 21.70 units; corners x 15..353 of 375; wheel-out stops at 32.56) |
| VIEW-15 | P2 | mouse | A resize keeps the direction but moves the camera back to the fitted distance ([the default view](../foundations/the-view.md#the-default-view)). | Board screen, 1280 × 720. | 1. Orbit to a different angle and wheel in to about 8 units.<br>2. Resize the window to 800 × 900. | The camera faces the board from the same direction as after step 1, at the distance that fits the whole cube in the new window (well over 8 units). | pass (before resize 8.24 units; after, direction changed 0.003°, distance 15.22 (fit for 800x900: 15.22); all corners in frame) |
| VIEW-16 | P2 | second player | With reduced motion requested, moves do not glide or fade ([motion](../foundations/the-view.md#motion)). | New game; White's page emulating `prefers-reduced-motion: reduce` (devtools rendering settings). | 1. White plays `Bc1-Ec4`. | On White's page the Queen is on `Ec4` at once and the Black pawn is gone at once, with teal on `Bc1` and `Ec4`. Black's page (no emulation) shows the glide and fade. | pass (White (reduced motion): no glide, no fade, Queen on Ec4, teal on both cells; Black: glide and fade) |

Not checkable by hand:

- A glide that resumes when a hidden tab is shown again: headless and occluded windows stop drawing, so this needs a real browser and a watchful tester.
- Orbit stopping at the poles, and looking up from under the board: checkable, but the claim is the 3D library's default, not the product's; low value.

Verified against 3D Chess commit `4e18386`
