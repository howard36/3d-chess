# Verification: start

How to run this file: bring up the local server and client as in [the protocol](README.md#how-to-run-a-pass). Each section starts from fresh browser contexts (no stored seats) unless it says otherwise. Device values are defined in [devices and conditions](README.md#devices-and-conditions); `server stopped` means the local server process is not running (stop it with Ctrl+C and start it again when the step says so), and `storage off` means the browser refuses to store site data for `localhost`.

## start/creating-a-game.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CREATE-01 | P1 | mouse | Clicking "Start New Game" shows "Creating Game..." and then the share-link screen at `/game/{id}` ([the simple case](../start/creating-a-game.md#the-simple-case)). | Start screen, connected. | 1. Click "Start New Game". | The button reads "Creating Game..." and is disabled, then the address becomes `/game/` plus six characters from A–Z and 0–9, and the page reads "Game created! Share this link with a friend:" with that link and a "Copy link" button. | — |
| CREATE-02 | P1 | server stopped | The button works before the connection is open; the click is queued ([end without sending](../start/creating-a-game.md#end-without-sending)). | Server stopped; open `/`. | 1. Note the status line.<br>2. Click "Start New Game".<br>3. Start the server. | 1: "Connecting to server…", then "Reconnecting to server…". 2: "Creating Game...", disabled, status line still shown. 3: within about 8 s, the share-link screen of a new game. | — |
| CREATE-03 | P1 | drop | A create whose answer is lost to a drop is re-sent when the connection returns, and the page reaches a new game ([while in flight](../start/creating-a-game.md#while-in-flight), [cancel and interrupt](../start/creating-a-game.md#cancel-and-interrupt)). | Start screen, connected. | 1. Click "Start New Game" and cut the connection before the answer arrives.<br>2. Look at the button while the connection is down.<br>3. Let the connection come back. | 2: "Creating Game...", disabled, with "Reconnecting to server…" under it. 3: within about 8 s of the connection returning, the address becomes `/game/{id}` and the share-link screen shows; no reload was needed. | — |
| CREATE-04 | P2 | mouse | A double click creates one game ([while in flight](../start/creating-a-game.md#while-in-flight)). | Start screen, connected. | 1. Double-click "Start New Game". | One navigation to a game page; no error. | — |
| CREATE-05 | P2 | keyboard | The button works from the keyboard ([modifiers](../start/creating-a-game.md#modifiers)). | Start screen, nothing focused. | 1. Press Tab.<br>2. Press Enter. | 1: the button is focused, with a focus ring. 2: a game is created. | — |
| CREATE-06 | P1 | storage off | With storage refused, the creator lands on the join screen, and "Join Game" gives "Already in a game" (suspected bug) ([edge cases](../start/creating-a-game.md#edge-cases)). | A context whose storage throws or is blocked. | 1. Click "Start New Game".<br>2. Click "Join Game". | Record what happens. Per the document: 1: the join screen instead of the share link. 2: "Joined game, waiting for start..." with "Error: Already in a game". | — |
| CREATE-07 | P2 | second player | Arriving from a finished game does not bounce back into it ([edge cases](../start/creating-a-game.md#edge-cases)). | A finished game. | 1. Click "Start new game".<br>2. Wait 2 s. | The start screen stays, with "Start New Game" enabled and no error. | — |
| CREATE-08 | P2 | storage off, drop | With storage refused, a creator who clicked "Join Game" and then drops gets the creator's own seat back, and waits on the joined screen ([edge cases](../start/creating-a-game.md#edge-cases)). | CREATE-06 after step 2. | 1. Cut the connection and let it come back.<br>2. Join from a second context. | 1: "Joined game, waiting for start..." stays (the old "Error: Already in a game" still in the banner); no board. 2: the creator's page shows the board, with the color the second context does not have. | — |

Not checkable by hand:

- Back pressed within the round trip of a create: a race of a few tens of milliseconds.
- How long "Connecting to server…" lasts on a cold production server.
- Whether a re-sent create left a second, unused game on the server: nothing in the product shows it.

## start/waiting-for-an-opponent.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WAIT-01 | P1 | mouse | The share link is plain text, "Copy link" is the screen's only control, and the screen shows no color ([the simple case](../start/waiting-for-an-opponent.md#the-simple-case)). | A game just created, on `localhost`. | 1. Look for controls and click the link text.<br>2. Look for the creator's color. | 1: one button, "Copy link", and no link element; clicking the text does nothing. 2: the color is not shown anywhere. | — |
| WAIT-02 | P1 | second player | A join replaces the screen with the board, and the color appears then ([the answer arrives](../start/waiting-for-an-opponent.md#the-answer-arrives)). | WAIT-01. | 1. Join from a second context. | The creator's page shows the board screen, "You are playing as …" with the stored color, "White to move", then "Opponent: online". | — |
| WAIT-03 | P1 | mouse | Reloading while waiting returns to the share-link screen ([begin](../start/waiting-for-an-opponent.md#begin)). | A game with one seat taken. | 1. Reload. | The share-link screen again, with the same link. | — |
| WAIT-04 | P1 | second player | A join made while the creator is away is found on return ([the answer arrives](../start/waiting-for-an-opponent.md#the-answer-arrives)). | A game with one seat taken. | 1. Close the creator's page.<br>2. Join from a second context.<br>3. Reopen the link in the creator's context. | 2: the joiner's board shows "Opponent: offline". 3: the creator's page shows the board directly (after the brief share-link screen), and the joiner's shows "Opponent: online". | — |
| WAIT-05 | P1 | second tab | Opening the link in another tab of the same browser takes the seat ([edge cases](../start/waiting-for-an-opponent.md#edge-cases)). | A game with one seat taken. | 1. Open the link in a new tab of the same context. | The new tab shows the share-link screen; the first shows "This game is open in another tab". | — |
| WAIT-06 | P1 | drop | Back then Forward before the start screen's connection opens sends one rejoin, and the share-link screen returns without an error ([edge cases](../start/waiting-for-an-opponent.md#edge-cases)). | A game with one seat taken; the connection held down so that it cannot open. | 1. Press Back.<br>2. Press Forward.<br>3. Let the connection open. | 3: the share-link screen, no error banner; the page is still the creator's (a later join from a second context brings the board). | — |
| WAIT-07 | P2 | drop | A drop while waiting shows "Reconnecting…" and the screen stays ([cancel and interrupt](../start/waiting-for-an-opponent.md#cancel-and-interrupt)). | A game with one seat taken. | 1. Drop the connection and hold it for 3 s.<br>2. Release it. | 1: "Reconnecting…" at the top right, the share-link text unchanged. 2: the box goes; the screen stays. | — |
| WAIT-08 | P1 | mouse | "Copy link" puts the share link on the clipboard and says "Copied" ([end without sending](../start/waiting-for-an-opponent.md#end-without-sending)). | A game just created, on `localhost`. | 1. Click "Copy link".<br>2. Paste the clipboard into the address bar of another context (or read it back). | 1: "Copied" appears beside the button. 2: exactly the link shown in the box, `{origin}/game/{id}`. | — |
| WAIT-09 | P2 | keyboard | "Copy link" is reached with Tab and works with Enter ([modifiers](../start/waiting-for-an-opponent.md#modifiers)). | A game just created, on `localhost`, nothing focused. | 1. Press Tab.<br>2. Press Enter. | 1: "Copy link" is focused, with a focus ring. 2: "Copied". | — |
| WAIT-10 | P2 | narrow | In a 375 px window the link wraps inside its box and nothing runs off the screen ([interactions](../start/waiting-for-an-opponent.md#interactions-with-other-systems)). | A game just created, in a 375 px window. | 1. Look at the title, the link, and "Copy link".<br>2. Try to scroll sideways. | 1: the link breaks across lines inside the dark box; the title fits on one line; "Copy link" is fully visible. 2: no horizontal scroll. | — |
| WAIT-11 | P3 | touch | On a plain `http://` address other than `localhost`, "Copy link" is not shown ([end without sending](../start/waiting-for-an-opponent.md#end-without-sending)). | The dev server opened from another device by the machine's network address (`http://192.168.…:5173`); a game just created there. | 1. Look under the link. | No "Copy link" button; the link in its box only. | — |
| WAIT-12 | P2 | drop | Back, then Forward while the start screen's connection is reconnecting after having opened, gives "Error: Already in a game" (suspected bug) ([edge cases](../start/waiting-for-an-opponent.md#edge-cases)). | A game with one seat taken. | 1. Press Back and wait for the start screen's connection to open (the status line goes).<br>2. Drop that connection and hold it.<br>3. Press Forward.<br>4. Release the connection. | Record what happens. Per the document: 4: the share-link screen with "Error: Already in a game" in the banner; the seat is unaffected. | — |

Not checkable by hand:

- The one-instant "Opponent: offline" as the board appears after a rejoin while waiting: shorter than a frame.
- Whether reopening an unjoined game keeps it from expiring.
- "Could not copy; select the link instead": needs the browser to refuse the clipboard write, which depends on the browser's permission settings.

## start/joining-a-game.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| JOIN-01 | P1 | mouse | A visitor sees only "Join Game" ([begin](../start/joining-a-game.md#begin)). | A game with one seat taken; a fresh context. | 1. Open the link. | The title and a "Join Game" button; nothing else. | — |
| JOIN-02 | P1 | second player | Joining starts the game on both pages, with the joiner holding the other color ([the answer arrives](../start/joining-a-game.md#the-answer-arrives)). | JOIN-01. | 1. Click "Join Game". | At most a flash of "Joined game, waiting for start...", then the board screen on both pages; the two seat labels name different colors; "White to move"; "Opponent: online" on both. | — |
| JOIN-03 | P1 | mouse | An unknown id is refused with "Cannot join" ([the answer arrives](../start/joining-a-game.md#the-answer-arrives)). | A fresh context. | 1. Open `/game/NOPE00`.<br>2. Click "Join Game". | "Error: Cannot join" in the banner; the join screen again. | — |
| JOIN-04 | P1 | second player | A third person is refused with "Game full" ([edge cases](../start/joining-a-game.md#edge-cases)). | A started game; a third context. | 1. Open the link.<br>2. Click "Join Game". | "Error: Game full"; the join screen again. | — |
| JOIN-05 | P1 | drop | A join whose answer is lost to a drop is re-sent when the connection returns, ends on the board, and the seat survives a reload ([while in flight](../start/joining-a-game.md#while-in-flight), [cancel and interrupt](../start/joining-a-game.md#cancel-and-interrupt)). | A game with one seat taken; a fresh context on its link. | 1. Click "Join Game" and cut the connection before the answer.<br>2. Let it reconnect.<br>3. Reload the joiner's page. | 1: "Joined game, waiting for start..." with "Reconnecting…". 2: the board screen, with the color the creator's page does not have; the creator's page shows the board and, finally, "Opponent: online"; no "Game full". 3: the board again, same color. | — |
| JOIN-06 | P2 | second player | Joining works while the creator is away ([edge cases](../start/joining-a-game.md#edge-cases)). | A game whose creator's page is closed. | 1. Join. | The board screen with "Opponent: offline". | — |
| JOIN-07 | P2 | second tab | A second tab still on the join screen is refused after the first joins ([edge cases](../start/joining-a-game.md#edge-cases)). | A visitor with the link open in two tabs of one context. | 1. Join in tab 1.<br>2. Click "Join Game" in tab 2. | 2: "Error: Game full" in tab 2. | — |
| JOIN-08 | P1 | drop | After a lost join, a reload of the same tab shows the join screen, and "Join Game" there gets the same seat back ([cancel and interrupt](../start/joining-a-game.md#cancel-and-interrupt)). | A game with one seat taken; a fresh context on its link. | 1. Click "Join Game" and cut the connection before the answer, keeping it down.<br>2. Reload the page with the connection allowed again.<br>3. Click "Join Game". | 2: the join screen (nothing was stored). 3: the board screen with the free seat's color; no "Game full"; the creator's page shows "Opponent: online". | — |
| JOIN-09 | P2 | drop, second tab | After a lost join, a different tab of the same browser is refused with "Game full" ([open questions](../start/joining-a-game.md#open-questions-and-verification)). | JOIN-08 after step 1, with the first join known to have reached the server (the creator's page shows the board). | 1. Close the tab.<br>2. Open the link in a new tab of the same context.<br>3. Click "Join Game". | 2: the join screen. 3: "Error: Game full". | — |
| JOIN-10 | P2 | drop, second player | A re-sent join after the creator has moved shows the real position ([the answer arrives](../start/joining-a-game.md#the-answer-arrives)). | A game with one seat taken; a fresh context on its link. | 1. Click "Join Game" and cut the joiner's connection before the answer, keeping it down. The creator's page shows the board; if it says "You are playing as black.", start again with a new game.<br>2. White (the creator) plays `Bb2-Bb3`.<br>3. Let the joiner reconnect. | 3: the joiner's board shows the pawn already on `Bb3`, with no glide, "Black to move", and "1. Bb2–Bb3" in the move list; the creator's page shows "Opponent: online". | — |

Not checkable by hand:

- The one-frame return to the join screen when "Join Game" is clicked again after a refusal.
- Whether a visitor should learn anything about the game before clicking: a product call.
- Whether "reopen closed tab" restores a tab's client id: browser-specific.

Verified against 3D Chess commit `90142a3`
