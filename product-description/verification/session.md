# Verification: session

How to run this file: bring up the local server and client as in [the protocol](README.md#how-to-run-a-pass). Unless a row says otherwise, the setup is a started game in two browser contexts. Most rows need `drop`: cutting and restoring one page's connection while the server keeps the game, which only [the harness](harness/README.md) (or a server that keeps its games across restarts) can do faithfully; see [devices and conditions](README.md#devices-and-conditions). `delayed answers` means the harness holds back the messages a page receives for a few seconds after its connection opens, which widens a window that is normally one round trip long.

## session/reload-and-return.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RELOAD-01 | P1 | second player | A reload restores the game without glides, at the default view ([the answer arrives](../session/reload-and-return.md#the-answer-arrives)). | Play `Bc1-Ec4`; orbit White's view. | 1. Reload White's page. | The board with the Queen on `Ec4`, teal on `Bc1` and `Ec4`, no glide, "Black to move — in check" (the Queen on `Ec4` checks Black), "1. Bc1–Ec4" in the list, the default view, "Opponent: online". | pass (turn indicator "Black to move — in check"; camera after reload 7.97,6.13,10.42 = first view (off by 0.000); no glide frame sampled) |
| RELOAD-02 | P1 | mouse | An expired game's rejoin is refused, the stored seat is deleted, and "Join Game" then gives "Cannot join" without the joined screen (suspected bug) ([edge cases](../session/reload-and-return.md#edge-cases)). | A game the server no longer has (restart the local server). | 1. Reload the game page.<br>2. Click "Join Game". | 1: "Error: Cannot rejoin" and the join screen. 2: record what happens; per the document, "Error: Cannot join" with no "Joined game, waiting for start..." in between. | pass (joined screen observed in 1 of 40 samples at 25 ms; ended on "Error: Cannot join") |
| RELOAD-03 | P1 | mouse | A jump through history from one game page straight to another shows the target game under its own address ([edge cases](../session/reload-and-return.md#edge-cases)). | One context that has played in game A until it ended, returned to the start screen, and created game B (B's page shows the share-link screen). | 1. From B's page, jump two entries back in the history menu to A's page (without passing through the start screen). | The address is A's, and the page shows game A: the board with A's final position and A's end-game dialog, after at most a flash of the share-link screen. No trace of game B. | pass (jumped from B P3DUMZ to A NLHIKN: A's board, final position and end-game dialog; no trace of B) |
| RELOAD-04 | P2 | second tab | Reloading the replaced tab takes the seat back ([edge cases](../session/reload-and-return.md#edge-cases)). | The game open in two tabs of White's context; the first replaced. | 1. Reload the first tab. | The first tab shows the board; the second shows "This game is open in another tab". | pass |
| RELOAD-05 | P1 | second player | A history jump away from a page that saw its own game start live leaves the target game's stored seat alone and rejoins it with its own color ([edge cases](../session/reload-and-return.md#edge-cases)). | One context (P) and a second player (Q). P creates game B, Q joins it, and P notes P's color in B; P returns to the start screen without loading a new page (the browser's Back button, as many times as needed; typing the address loads a fresh page and does not test this). P creates game A and waits on its share-link screen until Q joins A; repeat A until P's color in A differs from P's color in B. Q keeps both games open. | 1. From A's board, P jumps two entries back in the history menu to B's page (without passing through the start screen).<br>2. Read P's stored seat for B (`localStorage['3dchess:role:{B}']`). | 1: P's page for B shows B's board with "You are playing as" P's color in B, no replaced dialog; Q's page for B shows no dialog and "Opponent: online". 2: P's color in B, unchanged. | pass (P was white in B, black in A (after 1 game(s)); after the jump P shows B as white, stored seat white; start screen reached by an in-page history push (no link exists), Back used to discard an A with the same color) |
| RELOAD-06 | P2 | drop, delayed answers | A reload whose first rejoin answer is lost still takes over on the next connection ([send](../session/reload-and-return.md#send)). | The game open in two tabs of White's context; the first replaced. | 1. Reload the first tab and cut its connection before the snapshot arrives, keeping it down; then reload the second tab so that it holds the seat.<br>2. Release the first tab's connection. | 2: the first tab shows the board and holds the seat; the second tab shows "This game is open in another tab". | pass (first tab's rejoins (takeover): [true,true]; first answer lost by discarding incoming messages, then cut (first rejoin takeover true)) |

Not checkable by hand:

- Whether a rejoin counts as activity for the ~30-day expiry.
- Back-forward cache restores: browser-specific.

## session/connection-loss.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DROP-01 | P1 | drop | During a drop: "Reconnecting…", no input, everything else frozen; after it, the page catches up and the opponent sees offline then online ([begin](../session/connection-loss.md#begin), [the answer arrives](../session/connection-loss.md#the-answer-arrives)). | White to move, `Bb2` selected. | 1. Drop White's connection and hold it.<br>2. Look at both pages.<br>3. Release it. | 2: White: "Reconnecting…", selection gone, "Move" disabled; Black: "Opponent: offline". 3: box gone; Black: "Opponent: online"; White can move. | pass ("Move" checked disabled with "Bb2-Bb3" typed during the drop) |
| DROP-02 | P1 | drop, delayed answers | The board takes no input between the reconnect and the snapshot, so no move can be played against the old position ([while in flight](../session/connection-loss.md#while-in-flight)). | White to move. | 1. White plays `Bb2-Bb3`, and White's connection is cut before the echo arrives.<br>2. Black replies `Ed4-Ed3`.<br>3. Restore White's connection with its answers delayed; check that "Reconnecting…" has gone.<br>4. On White's still-stale board, press `Bb2`, then try `Bb2-Bb3` in the move box. | 4: nothing is selected; "Move" stays disabled; nothing is sent. When the answers arrive: White's board shows `Bb3` in place and `Ed4-Ed3` gliding in, "White to move", no frozen-board banner; the game continues normally on both boards. | pass (stale checks done 0.9 s into a 9 s hold: no selection, "Move" disabled, nothing sent (["rejoin_game"]); then Bb3 in place, Ed4-Ed3 glided in, play continued: "1. Bb2–Bb3 Ed4–Ed32. Bc2–Bc3 Ec4–Ec3") |
| DROP-03 | P2 | drop | A server fault or any close other than a replacement is retried ([begin](../session/connection-loss.md#begin)). | Board screen. | 1. Close White's connection with any code. | "Reconnecting…", then recovery. | pass (closed from the page with code 4000; retried, recovered, and White then moved) |
| DROP-04 | P2 | server stopped | Restarting the local server loses its games; a mid-game page keeps its board, shows "Cannot rejoin", and takes no input ([the answer arrives](../session/connection-loss.md#the-answer-arrives)). | Board screen, White to move. | 1. Restart the local server.<br>2. Wait for the reconnect.<br>3. White presses a piece, and types a move in the move box. | 2: "Error: Cannot rejoin"; the board stays. 3: nothing is selected; "Move" stays disabled; nothing is sent. (Production keeps games across restarts; this row checks the page's handling only.) | pass (board kept; "Error: Cannot rejoin"; a press selected nothing, "Move" stayed disabled, and nothing was sent; stored seat kept) |
| DROP-05 | P1 | drop, second tab | A reconnect does not take the seat from another tab of the same browser: it shows the replaced dialog instead ([the answer arrives](../session/connection-loss.md#the-answer-arrives)). | White's game open in one tab. | 1. Drop White's connection and hold it.<br>2. Open the game in a second tab of White's context.<br>3. Release the first tab's connection. | 3: the first tab shows "This game is open in another tab" with "Play here" focused and no error banner; the second tab keeps the board and can move; Black never shows "Opponent: offline" after step 2. | pass (Black presence after the second tab opened: ["Opponent: online"]) |
| DROP-06 | P3 | narrow, drop | In a 375 px window "Reconnecting…" does not overlap the turn indicator ([interactions](../session/connection-loss.md#interactions-with-other-systems)). | Board screen in a 375 px window. | 1. Drop the connection and hold it. | The turn indicator alone on the top row; "Reconnecting…" at the right of the row below, next to the seat label; neither overlaps the other. | pass (turn 87,10 201x42; seat 10,60 174x92; reconnecting 198,60 167x40) |

Not checkable by hand:

- Laptop sleep: how long a browser takes to notice a dead connection.
- The one-hour limit.
- A fault on every rejoin retrying every half second: needs a faulty server.

## session/second-tab.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TAB-01 | P1 | second tab | "Play here" takes the seat back; moves made in the other tab glide in; the view angle is kept ([the answer arrives](../session/second-tab.md#the-answer-arrives)). | White's first tab replaced by a second tab; the first tab orbited before it was replaced. | 1. In the second tab, White plays `Bb2-Bb3`.<br>2. In the first tab, click "Play here". | The first tab: the dialog goes, the pawn glides to `Bb3`, the orbited angle is unchanged. The second tab shows the dialog. Black never shows "Opponent: offline". | pass (glide seen after "Play here"; camera moved 0.003; Black presence ["Opponent: online"]) |
| TAB-02 | P1 | second tab, drop | A tab that was reconnecting when the other took the seat does not take it back: it shows the replaced dialog ([edge cases](../session/second-tab.md#edge-cases)). | White's first tab on the board. | 1. Drop the first tab's connection and hold it.<br>2. Open the game in a second tab.<br>3. Release the first tab's connection. | 3: the first tab shows "This game is open in another tab" with "Play here" focused and no error banner; the second tab keeps the board, with no dialog, and can move. | pass |
| TAB-03 | P2 | second tab | Closing the newer tab leaves the older one replaced ([edge cases](../session/second-tab.md#edge-cases)). | First tab replaced. | 1. Close the second tab. | The first tab still shows the dialog; Black shows "Opponent: offline". | pass |
| TAB-04 | P2 | keyboard | The replaced dialog takes focus on "Play here" and cannot be dismissed; nothing behind it, including the end-game dialog, can be reached with Tab ([the replaced dialog](../session/second-tab.md#the-replaced-dialog), [open questions](../session/second-tab.md#open-questions-and-verification)). | A finished game; its first tab replaced. | 1. Read the focused element.<br>2. Press Escape; click the backdrop.<br>3. Press Tab repeatedly, reading the focused element. | 1: "Play here". 2: the dialog stays. 3: the only page element focus reaches is "Play here" (it may pass through the browser's own controls); never "Start new game", the move box, or the canvas. | pass (focus on open: BUTTON:Play here; after Escape and a backdrop click the dialog stayed; Tab x8: BUTTON:Play here -> BODY -> BUTTON:Play here -> BODY -> BUTTON:Play here -> BODY -> BUTTON:Play here -> BODY) |
| TAB-05 | P1 | second tab, drop | "Play here" after seat in use takes the seat back ([send](../session/second-tab.md#send)). | TAB-02 after step 3. | 1. In the first tab, click "Play here". | The first tab's dialog closes at once; a moment later the first tab has the board and can move. The second tab shows the dialog with "Play here" focused. Black never shows "Opponent: offline". | pass (dialog gone 3 ms after the click; first tab then moved Bc2-Bc3; Black presence ["Opponent: online"]) |
| TAB-06 | P2 | keyboard, second tab | On the share-link screen, the replaced dialog puts the page content and the error banner out of reach ([the replaced dialog](../session/second-tab.md#the-replaced-dialog)). | A game with one seat taken; its link opened in a second tab of the creator's context, so the first tab is replaced. | 1. In the first tab, read the focused element.<br>2. Press Tab repeatedly. | 1: "Play here". 2: the only page element focus reaches is "Play here"; never "Copy link" or an error banner's "✕". | pass (banner present ("Error: Already in a game✕"); focus on open: BUTTON:Play here; Tab x8: BODY -> BUTTON:Play here -> BODY -> BUTTON:Play here -> BODY -> BUTTON:Play here -> BODY -> BUTTON:Play here) |
| TAB-07 | P2 | second tab, drop | A tab showing the dialog after seat in use takes the free seat by itself on its next reconnect once the other tab has closed ([end without sending](../session/second-tab.md#end-without-sending)). | TAB-02 after step 3. | 1. Close the second tab; wait until Black shows "Opponent: offline".<br>2. Drop the first tab's connection and hold it.<br>3. Release it. | 2: the dialog gives way to "Reconnecting…". 3: without a click, the first tab has the board and can move; Black shows "Opponent: online". | pass (no click; rejoins sent by the first tab (takeover): [false,false]) |

Not checkable by hand:

- A move sent by the older tab at the instant of replacement: a race inside the server.
- The one-round-trip wait for the snapshot after "Play here": too short to see without delayed answers.

Verified against 3D Chess commit `4e18386`
