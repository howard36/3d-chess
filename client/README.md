# client

The React + Vite + react-three-fiber frontend. All documentation (rules, protocol,
architecture, coordinate systems, development commands) lives in the root
[README.md](../README.md); this directory has no separate docs.
