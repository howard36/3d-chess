import classic from '../classic';
import type { Design } from '../types';

// Placeholder until this design is built: the classic look under a new name.
const design: Design = { ...classic, id: 'hologram', name: 'Hologram' };

export default design;
